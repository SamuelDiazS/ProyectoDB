[project]
name = "proyectodb"
version = "0.1.0"
description = "Aplicación FastAPI para consultar una base de datos"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "asyncpg>=0.31.0",
    "fastapi[standard]>=0.139.2",
    "loguru>=0.7.3",
]
